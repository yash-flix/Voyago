
<nav class="navbar navbar-expand-md navbar-dark voyago-navbar sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/"><i class="fa-solid fa-mountain-sun"></i>
      VOYAGO
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" 
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav ms-auto">
        <a class="nav-link active" aria-current="page" href="/">Home</a>
        <a class="nav-link" href="/listings">All Listings</a>
        <a class="nav-link" href="/listings/new">Add new Listing</a>
        
      </div>
      <div class="navbar-nav ms-auto">
        <% if(!currUser){ %>
           <a class="nav-link " aria-current="page" href="/signUp">SignUp</a>
           <a class="nav-link" href="/login">Login</a>
          <% }%>
       <% if(currUser){ %>
        <a class="nav-link" href="/logout">Logout</a>
          <% }%>
       
      </div>
    </div>
  </div>
</nav>

<style>

.voyago-navbar {
  background: #ffffff; 
  border-bottom: 1px solid rgba(0,0,0,0.08); 
  box-shadow: 0 1px 6px rgba(0,0,0,0.05); 
  transition: background 0.3s ease;
}


.voyago-navbar .navbar-brand {
  font-weight: 700;
  font-size: 1.5rem;
  color: #222 !important;
  display: flex;
  align-items: center;
  gap: 6px;
  letter-spacing: 0.5px;
}

.voyago-navbar .fa-mountain-sun {
  color: #0072ff; 
  font-size: 1.3rem;
}

/* Nav Links */
.voyago-navbar .nav-link {
  color: #444 !important;
  font-weight: 500;
  margin-left: 1.2rem;
  position: relative;
  transition: color 0.2s ease-in-out;
}

/* Minimal hover: underline grows in */
.voyago-navbar .nav-link::after {
  content: "";
  position: absolute;
  bottom: -6px;
  left: 0;
  width: 0%;
  height: 2px;
  background: #0072ff;
  transition: width 0.25s ease;
}

.voyago-navbar .nav-link:hover {
  color: #0072ff !important;
}

.voyago-navbar .nav-link:hover::after {
  width: 100%;
}

/* Active link */
.voyago-navbar .nav-link.active {
  color: #0072ff !important;
  font-weight: 600;
}

/* Mobile toggler */
.voyago-navbar .navbar-toggler {
  border: none;
}

.voyago-navbar .navbar-toggler-icon {
  filter: invert(0.2);
}
</style>

